#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void wordSquare(char word[],int width,int height)
{
    char square[2*strlen(word) + (width-1)*6 - 1][strlen(word)+(strlen(word)-1)*(height-1)];

}
